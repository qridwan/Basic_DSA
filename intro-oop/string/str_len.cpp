#include <bits/stdc++.h>
using namespace std;

int main()
{
    string st = "ridwan"; // dynamic array;
    cout << st.size() << endl;
    // st.resize(4);
    // st.clear();
    st.empty(); // return boolean
    cout << st.size() << endl;
    return 0;
}